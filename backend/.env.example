# YouTube Music API Configuration
# You can optionally provide OAuth credentials for authenticated requests
# For now, we'll use the public API without authentication

# Server Configuration
HOST=0.0.0.0
PORT=8000
